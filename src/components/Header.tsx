import React from 'react';

const Header: React.FC = () => (
  <header style={{ background: '#026aa7', color: '#fff', padding: '16px', fontSize: '1.5rem', fontWeight: 'bold' }}>
    e-Kanban (Trello Style)
  </header>
);

export default Header;
